%nav.top-bar(data-topbar)
  %ul.title-area
    %li.name
      %h1
        %a(href="/") CS Tracker
  %section.top-bar-section
    %ul.right
      - if user_signed_in?
        = render 'menu/signed_in_top_bar_items'
      = render 'menu/sign_in_top_bar_section'
